# isys2120_Y2023_asm3
Analyze the TravelTimes table and define the relational schema of the data. Allows the user to display all rows of the corresponding table, display certain row contents that match the provided values, add rows, delete rows, update rows, and generate reports. and build related web pages.
